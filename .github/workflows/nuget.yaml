name: Publish NuGet Packages

on:
  workflow_dispatch:

jobs:
  publish:
    strategy:
      matrix:
        include:
          - dir: src/ScottPlot
            packageName: ScottPlot
          - dir: src/controls/ScottPlot.WinForms/ScottPlot.csproj
            packageName: ScottPlot.WinForms
          - dir: src/controls/ScottPlot.WPF/ScottPlot.WPF.NUGET.csproj
            packageName: ScottPlot.WPF
          - dir: src/controls/ScottPlot.Avalonia/ScottPlot.Avalonia.NUGET.csproj
            packageName: ScottPlot.Avalonia
    runs-on: windows-latest
    name: ${{ matrix.packageName }}
    steps:
      - name: 🛒 Checkout
        uses: actions/checkout@v2
      - name: 🐢 Set up MSBuild
        uses: microsoft/setup-msbuild@v1.0.3
      - name: 🚚 Restore
        working-directory: ${{ matrix.dir }}
        run: dotnet restore
      - name: 🐌 MSBuild
        working-directory: ${{ matrix.dir }}
        run: msbuild -property:Configuration=Release -verbosity:minimal
      - name: 🧪 Test
        working-directory: ${{ matrix.dir }}
        run: dotnet test --no-restore
      - name: 🚀 Publish
        working-directory: ${{ matrix.dir }}
        run: nuget push bin/Release/*.nupkg
