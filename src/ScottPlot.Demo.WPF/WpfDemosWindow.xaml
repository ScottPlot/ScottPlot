<Window x:Class="ScottPlot.Demo.WPF.WpfDemosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScottPlot.Demo.WPF"
        mc:Ignorable="d"
        Background="WhiteSmoke"
        WindowStartupLocation="CenterScreen"
        Title="ScottPlot WPF Demos" Height="300" Width="400">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="5" />
            <Setter Property="Margin" Value="5" />
        </Style>
    </Window.Resources>
    
    <StackPanel>

        <Label Content="WPF-Specific Demos" FontSize="24" FontWeight="SemiBold" />

        <DockPanel>
            <Button Content="Mouse Tracker" Click="LaunchMouseTracker"/>
            <TextBlock Text="Dispay mouse position in pixel and plot coordinates"/>
        </DockPanel>

        <DockPanel>
            <Button Content="Transparent Plot" Click="LaunchTransparentPlot"/>
            <TextBlock Text="Show a plot with a transparent background"/>
        </DockPanel>

    </StackPanel>
</Window>
